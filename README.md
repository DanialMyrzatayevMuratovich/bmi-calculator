# BMI Calculator with Node.js

This is a simple BMI (Body Mass Index) calculator built with **Node.js** and **Express**. The app calculates your BMI based on your weight and height, and then returns the result with a category (Underweight, Normal weight, Overweight, or Obesity).

## Features

- Input weight (in kilograms) and height (in meters).
- Calculate BMI and display the result with a category.
- Beautiful and responsive design using simple CSS.
- The app handles GET and POST HTTP requests using Express.

## Project Structure
BMI-CALCULATOR/
├── node_modules/         
├── app.js             
├── readme.md          
├── package-lock.json         
├── package.json        
├── node_modules/            
│   └── style.css   
## Requirements

- **Node.js**: Make sure you have Node.js installed on your machine. You can download it from the official website [here](https://nodejs.org/).

## Installation

1. Clone the repository to your local machine:
    ```bash
    git clone https://github.com/your-username/BMI-Calculator.git
    ```

2. Navigate into the project directory:
    ```bash
    cd BMI-Calculator
    ```

3. Install the dependencies:
    ```bash
    npm install
    ```

4. Start the server:
    ```bash
    node index.js
    ```

5. Open your browser and go to `http://localhost:3000/` to access the BMI calculator.

## Usage

1. On the homepage, enter your **weight** (in kilograms) and **height** (in meters).
2. Click the "Calculate BMI" button to calculate your BMI.
3. The result will be displayed on a new page with the calculated BMI and the corresponding category (Underweight, Normal weight, Overweight, or Obesity).
4. You can return to the main page by clicking on the "Return" link.

## Technology Stack

- **Node.js**: JavaScript runtime for server-side applications.
- **Express**: Web framework for Node.js to handle HTTP requests.
- **CSS**: Simple styles for the form and result page.

## Contributing

Feel free to fork this repository and submit pull requests if you'd like to contribute to this project!

## Author
**DanialMyrzatayevMuratovich**
https://github.com/DanialMyrzatayevMuratovich


